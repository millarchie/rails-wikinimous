<div class="container mt-4 p-2">
  <h1>Articles</h1>
  <ul>
    <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
    <% end %>
  </ul>
  <%= link_to "Add an Article", new_article_path, class: "btn btn-success my-2 col-2" %>
</div>
